# frontend.archived — superseded by apps/web

This directory is the secondary Vite/React workbench from Phase 1–5.
It was superseded by **apps/web** (Next.js 14, App Router) which is the
canonical frontend and the one built and deployed by CI.

The code is preserved here for historical reference but is **not**
built, tested, or deployed. Do not add new features here.

See apps/web/ for the active frontend.
