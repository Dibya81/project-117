# data/

Runtime + demo data.

- `demo/` — demo documents (e.g., a large maintenance manual PDF for the end-to-end demo)
- `fixtures/` — test fixtures

Uploaded documents are stored in `data/uploads/` (git-ignored, created at runtime).